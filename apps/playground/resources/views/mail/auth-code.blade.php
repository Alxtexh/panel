{{-- Plain text on purpose: a sign-in code or link is read on a phone, often in a
     client that strips HTML, and there is nothing here that benefits from
     markup. It also cannot render a tracking pixel or a remote image, which is
     the correct posture for a message containing a credential. --}}
{{ $heading }}

{{ $body }}

If you did not ask for this, you can ignore this message and nothing will change.
