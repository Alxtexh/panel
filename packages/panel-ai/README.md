# Panel AI module

Optional AI SDK integration for Panel.

```sh
composer require alxtexh-enterprise/panel-ai
```

The package supplies provider-backed embeddings and tenant-scoped conversation
hooks. Core Panel abstractions remain usable with the local hash embedder and
without the Laravel AI SDK.
