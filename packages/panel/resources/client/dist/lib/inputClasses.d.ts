/**
 * Input text and placeholder styling.
 *
 * Always include `font-normal` on placeholders so weight does not inherit from
 * a nearby `font-medium` label. Placeholders use muted foreground so they do
 * not compete with labels.
 */
export declare const INPUT_TEXT = "text-foreground font-normal";
export declare const INPUT_PLACEHOLDER = "placeholder:text-muted-foreground placeholder:font-normal";
/** Typed value and placeholder styling for native inputs and textareas. */
export declare const INPUT_COPY = "text-foreground font-normal placeholder:text-muted-foreground placeholder:font-normal";
