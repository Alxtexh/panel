/**
 * Secondary copy: descriptions, helper text, empty states, auth subtitles.
 *
 * Always include `font-normal` so weight does not inherit from a nearby
 * `font-medium` label, title, or active step control.
 */
export const MUTED_COPY = 'text-sm text-muted-foreground font-normal'

/** Compact helper lines under fields and in dense toolbars. */
export const MUTED_COPY_XS = 'text-xs text-muted-foreground font-normal'

/** Field help and inline hints that sit beside labels. */
export const MUTED_COPY_SNUG = 'text-xs text-muted-foreground font-normal leading-snug'
