{
    "compilerOptions": {
        "target": "ESNext",
        "module": "ESNext",
        "moduleResolution": "bundler",
        "lib": ["ESNext", "DOM", "DOM.Iterable"],
        "jsx": "preserve",
        "jsxImportSource": "vue",
        "strict": true,
        "noEmit": true,
        "isolatedModules": true,
        "esModuleInterop": true,
        "resolveJsonModule": true,
        "forceConsistentCasingInFileNames": true,
        "skipLibCheck": true
    },
    "//": [
        "BOTH HALVES. `inertia` was missing, and the cost was not type checking -",
        "the demo covers that transitively by importing it. The cost was LINTING:",
        "ESLint's typed rules ask the project service for each file, a file no",
        "tsconfig claims comes back as a parsing error rather than a rule failure,",
        "and ~100 packaged screens reported `was not found by the project service`",
        "instead of being checked. tsconfig.build.json deliberately still lists",
        "only src - it is rooted there and controls what declarations get emitted."
    ],
    "include": [
        "src/**/*.ts",
        "src/**/*.d.ts",
        "src/**/*.vue",
        "inertia/**/*.ts",
        "inertia/**/*.d.ts",
        "inertia/**/*.vue"
    ]
}
